[tool.uv.workspace]
members = [
  "pipelines",
  "packages/contracts/python",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pytest.ini_options]
testpaths = ["pipelines/tests", "packages/contracts/python/tests"]
